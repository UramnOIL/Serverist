ktor {
    deployment {
        port = 8080
        watch = [com.uramnoil.serverist]
    }
    application {
        modules = [com.uramnoil.serverist.ApplicationKt.module]
    }

    development = true
}

mail {
    host = localhost
    port = 25
    from = "test@serverist.com"
    user = ''
    password = ''
}

database {
    url = "jdbc:mysql://localhost:3306/serverist"
    driver = com.mysql.cj.jdbc.Driver
    user = develop
    password = develop
}